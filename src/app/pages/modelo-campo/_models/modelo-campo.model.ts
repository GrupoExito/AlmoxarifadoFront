export interface ModeloCampo {
  id?: number;
  tipo: number;
  titulo: string;
  nome_tabela_view: string;
  campo_resultado: string;
  campo_filtro: string;
  alinhamento: string;
  formatacao: string;
  coluna_view: string;
  coluna_arquivo: string;
}
